= form_for @protocol_template do |f|
  =render "errors/error", :obj => @protocol_template
  .actions
    = f.submit :class => "btn"
  .field
    = f.label :name
    %br
    = f.text_field :name
  .field
    = f.label :body
    %br
    = f.text_area :body, :style => "width: 50%;", :rows => 60
